{
  "name": "rmb-bond-rebalancer-tests",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Automated regression tests for the RMB bond fund rebalancer. The v1.0.1 algorithm behavior remains the protected baseline.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "golden:generate": "node tests/golden/generate.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
