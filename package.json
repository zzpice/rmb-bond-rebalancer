{
  "name": "rmb-bond-rebalancer-tests",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Automated test baseline for v1.0.1 of the RMB bond fund rebalancer. Contains test tooling only; no business logic.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "golden:generate": "node tests/golden/generate.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
